# ****** This Makefile generated by make_func

.c.o:
	$(CC) $(CFLAGS) -c $*.c -o $(OBJDIR)/$*.o

OBJ=$(OBJDIR)/interface.o

$(OBJDIR)/%.o: %.c $(OBJDIR)
	$(CC) -I$(OBJDIR) $(CFLAGS) -o $@ -c $<

all: $(OBJDIR) $(OBJ)
	ar rlcu mudlib.a $(OBJ)

$(OBJDIR):
	-mkdir $(OBJDIR)

clean:
	-rm -f $(OBJDIR)/*.o
	-rm -f packages.a