private int near(float a, float b) { return (a > b ? a - b : b - a) < 1e-9; }
void do_tests() {
    ASSERT(near(0.0, acos(1.0)));
    ASSERT(near(3.141592653589793, acos(-1.0)));
    ASSERT(catch(acos(2.0)));    // domain error
}
