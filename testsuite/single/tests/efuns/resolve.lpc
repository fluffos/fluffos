void got_addr(string host, string addr, int key) { }
void do_tests() {
    // Queues an async DNS lookup and returns a handle; the callback
    // fires after this test run, which is fine.
    int h = resolve("localhost", "got_addr");
    ASSERT(intp(h));
}
