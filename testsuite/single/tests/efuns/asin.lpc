private int near(float a, float b) { return (a > b ? a - b : b - a) < 1e-9; }
void do_tests() {
    ASSERT(near(0.0, asin(0.0)));
    ASSERT(near(1.5707963267948966, asin(1.0)));
    ASSERT(catch(asin(2.0)));    // domain error
}
