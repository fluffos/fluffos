void do_tests() {
#ifdef __DWLIB__
    ASSERT_EQ("hero", replace("zero", "z", "h"));
    // Array form: pairwise from/to list.
    ASSERT_EQ("cd", replace("ab", ({ "a", "c", "b", "d" })));
#endif
    ASSERT(1);
}
