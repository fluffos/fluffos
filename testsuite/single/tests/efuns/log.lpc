private int near(float a, float b) { return (a > b ? a - b : b - a) < 1e-9; }
void do_tests() {
  ASSERT(near(0.0, log(1.0)));
  ASSERT(near(1.0, log(2.718281828459045)));
  ASSERT(catch(log(0.0)));       // domain error
  ASSERT(catch(log(-1.0)));
}
