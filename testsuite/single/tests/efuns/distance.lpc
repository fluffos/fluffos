private int near(float a, float b) { return (a > b ? a - b : b - a) < 1e-9; }
void do_tests() {
  ASSERT(near(5.0, distance(({ 0, 0 }), ({ 3, 4 }))));
  ASSERT(near(0.0, distance(({ 1, 2, 3 }), ({ 1, 2, 3 }))));
}
