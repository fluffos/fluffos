void do_tests() {
  // Levenshtein edit distance.
  ASSERT_EQ(0, string_difference("abc", "abc"));
  ASSERT_EQ(1, string_difference("abc", "abd"));
  ASSERT_EQ(3, string_difference("kitten", "sitting"));
  ASSERT_EQ(3, string_difference("", "abc"));
}
