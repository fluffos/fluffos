private int near(float a, float b) { return (a > b ? a - b : b - a) < 1e-9; }
void do_tests() {
    ASSERT(near(0.0, sin(0.0)));
    ASSERT(near(1.0, sin(1.5707963267948966)));
    ASSERT(near(0.0, sin(3.141592653589793)));
    ASSERT(floatp(sin(1.0)));
}
