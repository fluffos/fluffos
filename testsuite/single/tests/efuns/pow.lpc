private int near(float a, float b) { return (a > b ? a - b : b - a) < 1e-9; }
void do_tests() {
    ASSERT(near(8.0, pow(2, 3)));
    ASSERT(near(1.0, pow(9, 0)));
    ASSERT(near(0.5, pow(2, -1)));
    ASSERT(near(3.0, pow(9, 0.5)));
}
