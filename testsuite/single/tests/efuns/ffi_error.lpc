#ifdef __PACKAGE_FFI__
#include <ffi.h>
void do_tests() {
    ASSERT(stringp(ffi_error()));
    // A failed load records a message.
    ffi_load("/no/such/library.so.999");
    ASSERT(strlen(ffi_error()) > 0);
#endif
    ASSERT(1);
}
