#define TESTFILE "/testfile"

void do_tests() {
    rm(TESTFILE);
    ASSERT(file_size(TESTFILE) == -1);
    ASSERT(cp("/single/master.lpc", TESTFILE));
    ASSERT(file_size(TESTFILE));
    ASSERT(read_file("/single/master.lpc") == read_file(TESTFILE));
}

