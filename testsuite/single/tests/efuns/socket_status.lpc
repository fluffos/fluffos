#ifdef __PACKAGE_SOCKETS__
#include <socket.h>
void rcb(int fd) {}
void do_tests() {
  int fd = socket_create(STREAM, "rcb");
  mixed *all = socket_status();
  ASSERT(arrayp(all));
  ASSERT(arrayp(socket_status(fd)));
  socket_close(fd);
}
#endif
