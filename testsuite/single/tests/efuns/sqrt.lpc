private int near(float a, float b) { return (a > b ? a - b : b - a) < 1e-9; }
void do_tests() {
    ASSERT(near(3.0, sqrt(9)));
    ASSERT(near(1.4142135623730951, sqrt(2)));
    ASSERT(catch(sqrt(-1.0)));   // domain error
}
