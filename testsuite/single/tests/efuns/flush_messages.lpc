void do_tests() {
  // Flushing a non-interactive (or everyone, with none connected)
  // must not crash; an error about the non-interactive is acceptable.
  catch(flush_messages());
  catch(flush_messages(this_object()));
  ASSERT(1);
}
